# ETL-процесс для банковских данных

## Задания

### Задание №1

В некотором банке внедрили новую frontend-систему для работы с клиентами, а так же обновили и саму базу данных. Большую часть данных успешно были перенесены из старых БД в одну новую централизованную БД. Но в момент переключения со старой системы на новую возникли непредвиденные проблемы в ETL-процессе, небольшой период (конец 2017 начало 2018 года) так и остался в старой базе. Старую базу отключили, а не выгруженные данные сохранили в csv-файлы. Недавно банку потребовалось построить отчёт по 101 форме. Те данные что остались в csv-файлах тоже нужны. Загрузить их в новую БД не получиться из-за архитектурных и управленческих сложностей, нужно рассчитать витрину отдельно. Но для этого сначала нужно загрузить исходные данные из csv-файлов в детальный слой (DS) хранилища в СУБД PostgreSQL.

### Задача 1.1
**Разработать ETL-процесс для загрузки «банковских» данных из csv-файлов в соответствующие таблицы СУБД PostgreSQL.**

**Выполнено в:** `dags/dag_etl`, `sql/tables dm_ds.sql` и `sql/table_logs.sql`


### Задача 1.2
**Расчёт витрины оборотов (DM.DM_ACCOUNT_TURNOVER_F) и витрины остатков (DM.DM_ACCOUNT_BALANCE_F).**

**Выполнено в:** `sql/procedures_1.2.sql`

**Созданные процедуры:**
- `ds.fill_account_turnover_f(i_OnDate DATE)` — расчёт витрины оборотов
- `ds.fill_account_balance_f(i_OnDate DATE)` — расчёт витрины остатков

### Задача 1.3
**Расчёт 101 формы за январь 2018 года (DM.DM_F101_ROUND_F).**

**Выполнено в:** `sql/procedure_1.3.sql`

**Созданная процедура:**
- `dm.fill_f101_round_f(i_OnDate DATE)` — отчётная дата = первый день месяца, следующего за отчётным

### Задача 1.4
**Экспорт и импорт данных в CSV-формат.**

**Выполнено в:** `export_1.4.py` и `import_1.4.py`

---

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| **ETL-процесс** | Python + Apache Airflow |
| **База данных** | PostgreSQL |
| **Оркестрация** | Docker Compose (Airflow + PostgreSQL) |
| **Логирование** | Кастомная таблица в схеме LOGS |
| **Экспорт/импорт** | Python (csv, psycopg2) |
